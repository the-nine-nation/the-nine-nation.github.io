# 公共组件

- `SiteHeader.tsx`：品牌、主导航、登录状态和管理员入口
- `ReportView.tsx`：阶段报告与已保存报告共用的完整展示
- `PrintButton.tsx`：调用浏览器打印能力，支持另存为 PDF
- `ParticipantGate.tsx`：免登录的人物卡小窗；昵称与包容式性别选项仅保存在当前浏览器，性别不参与测评计分

组件应保持数据输入明确，不在组件内直接访问数据库。报告字段的兼容性默认值集中在 `ReportView`，用于继续展示旧版本报告；新增报告字段时应保持可选渲染，避免历史数据导致页面崩溃。
