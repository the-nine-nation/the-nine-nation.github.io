# 回归测试

`assessment.test.mjs` 直接加载 TypeScript 测评引擎，覆盖：

- 题库 ID、选项和计分结构
- 三轮 10 / 15 / 20 题数量
- 累计答案与自适应题序验证
- 篡改、缺题和非法选项拒绝
- 不同早期信号产生不同后续题目
- 最终报告的公开字段完整性

`npm test` 会先执行生产构建，再运行这些测试；`npm run check` 还会在此之前执行 ESLint。新增业务规则时应同时增加回归断言。
