# D1 迁移

本目录由 Drizzle Kit 管理，并在 Sites 打包时随部署版本一起发布。

- SQL 文件按编号顺序执行。
- `meta/` 保存迁移快照与日志，必须与 SQL 一起提交。
- 新模式通过修改 `db/schema.ts` 后运行 `npm run db:generate` 生成。
- 不要重写已在生产执行过的迁移；需要修正时新增下一条迁移。

发布前应确认迁移不会删除现有用户报告，并运行完整检查。
